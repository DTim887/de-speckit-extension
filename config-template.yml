# de-speckit-extension 配置
# 安装时会被复制为 .specify/extensions/de-speckit-extension/de-speckit-extension-config.yml
#
# 目前没有可配置项 —— speckit.de-speckit-extension.read-jira-ticket
# 是挂在 before_specify 上的强制门槛（始终启用，见 extension.yml），
# 没有开关可切。以后给这个扩展新增命令时，把对应的配置项加在这里。
