# de-speckit-extension configuration
# Copied to .specify/extensions/de-speckit-extension/de-speckit-extension-config.yml on install
#
# No configurable options yet — speckit.de-speckit-extension.read-jira-ticket
# is a mandatory, always-on gate on before_specify (see extension.yml) and
# has nothing to toggle here. Add per-command settings below as new
# commands are added to this extension.
